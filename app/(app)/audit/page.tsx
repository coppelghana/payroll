import { db } from "@/lib/db";
import { date } from "@/lib/format";

type Audit={id:number;event_type:string;entity_type:string;entity_id:string;actor_name:string;actor_role:string;description:string;metadata:Record<string,unknown>;created_at:string};

export default async function AuditPage(){const rows=await db()`SELECT id,event_type,entity_type,entity_id,actor_name,actor_role,description,metadata,created_at FROM audit_log ORDER BY created_at DESC LIMIT 250` as Audit[];return <><div className="page-head"><div><span className="eyebrow">Immutable evidence</span><h1>Audit trail</h1><p>Append-only records for access, master data, calculations and approvals.</p></div><span className="badge green">{rows.length} events</span></div><section className="card"><div className="table-wrap"><table><thead><tr><th>Date & time</th><th>Event</th><th>Description</th><th>Actor</th><th>Entity</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{date(r.created_at)}</td><td><span className="badge blue">{r.event_type.replaceAll("_"," ")}</span></td><td>{r.description}</td><td><b>{r.actor_name}</b><small>{r.actor_role}</small></td><td>{r.entity_type}<small>{r.entity_id}</small></td></tr>)}</tbody></table></div></section></>}
